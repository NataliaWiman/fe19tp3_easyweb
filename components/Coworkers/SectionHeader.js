import * as header from "./SectionHeader.styled";

const SectionHeader = ({ data }) => {
  return (
    <header.Container>
      <header.TextBox>
        <h4>IGNITE YOUR PASSION</h4>
      </header.TextBox>
    </header.Container>
  );
};

export default SectionHeader;
