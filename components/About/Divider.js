import * as divider from "./Divider.styled";

const Divider = ({ data }) => {
  return (
    <divider.Container>
      <divider.TextBox>
        <h4>DESIGN IS OUR PASSION</h4>
      </divider.TextBox>
    </divider.Container>
  );
};

export default Divider;
